import { CharacterCard } from "..";

describe("CharacterCard component", () => {
  it.todo("should render default information");
  it.todo("should render unspecified type when character type is empty");
});
